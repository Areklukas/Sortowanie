#include <iostream>
#include <cstdlib>
#include <cstring>

using namespace std;
int a;
int i;
int main()
{
    string(w) = "abacbede";

        for (int a=0; a<w.length()-1; a++)

        for (int i=0; i<w.length()-1; i++)

        if (w[a]>w[a+1])

        swap(w[a], w[a+1]);


    cout <<"Sortowanie babelkowe:" <<endl;
    cout <<w <<endl;

return 0;
}
